import React from "react"

import CentralText from "../components/CentralText"

export default ({ navigation }) => {
    return (
        <CentralText bColor="#fff" tColor="#000">
            Perfil
        </CentralText>
    )
}